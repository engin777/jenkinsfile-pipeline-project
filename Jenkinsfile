pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Clarusway_Way to Reinve Yourself'
                sh 'python --version'
                sh 'python pipeline.py'
                sh 'git version'
            }
        }
    }
}